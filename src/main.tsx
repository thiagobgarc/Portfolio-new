import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'

import { AppNavigation } from './app_navigation/app_navigation'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AppNavigation />
  </React.StrictMode>,
)
